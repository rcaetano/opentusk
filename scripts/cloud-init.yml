#cloud-config
# Minimal cloud-init: install base packages and update the system.
# All security hardening and user setup is handled by deploy-do.sh
# via SSH after the droplet is ready.

package_update: true
package_upgrade: true

packages:
  - fail2ban
  - ufw
  - rsync
  - curl
  - git
