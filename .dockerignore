# Exclude upstream repos (only poseidon/ is needed by Dockerfile.poseidon)
openclaw/

# Exclude docs and scripts (not used in Docker builds)
docs/
scripts/
*.md
LICENSE

# Exclude git metadata
.git
**/.git

# Exclude build artifacts and caches
**/node_modules
**/.cache
**/.turbo
**/dist

# Exclude local config
.openclaw/
.env
